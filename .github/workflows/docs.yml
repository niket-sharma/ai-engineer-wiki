name: Deploy docs
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install mkdocs-material
      - name: Convert wiki-links to Markdown
        run: python scripts/wikilinks_to_md.py --no-backup
      - run: mkdocs gh-deploy --force
